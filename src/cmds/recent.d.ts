/**
 * DCP Recent Command
 *
 * Adjust the recency threshold (number of recent messages to always keep).
 */
import type { CommandDefinition, DcpConfig } from "../types.js";
export declare function createRecentCommand(config: DcpConfig): CommandDefinition;
//# sourceMappingURL=recent.d.ts.map